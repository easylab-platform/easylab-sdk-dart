name: easylab_client_sdk
description: "EasyLab typed client SDK for Dart/Flutter — buf-generated Connect code (easylab.v1 + agent.v1 + worker.v1). No hand-written wrappers."
version: 0.5.0
publish_to: 'none'

environment:
  sdk: ^3.7.0

dependencies:
  connectrpc: ^2.0.0
  protobuf: ^6.0.0
  fixnum: ^1.1.1
